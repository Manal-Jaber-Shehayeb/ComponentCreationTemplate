const {getComponentProps}= require('./scripts/getComponentProps');
const {getComponentsNames} = require('./scripts/getComponentsNames');
module.exports = {
  getComponentProps,
  getComponentsNames
};
